# Server
PORT=4000
CLIENT_URL=http://localhost:5173

# PostgreSQL (Render/Railway will give you a connection string - paste it below)
DATABASE_URL=postgresql://username:password@localhost:5432/skribbl_clone

# Set to "true" if your DB provider requires SSL (Render/Railway prod usually does)
DB_SSL=false
